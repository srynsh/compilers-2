# This code has a syntax error

ink bubbleSort(num n)->void {
    num temp

    num[5] arr = {5, 1, 4, 2, 8}

    spool (num i = 0; i < n - 1; i++) {
        spool (num j = 0; j < n - i - 1; j++) {
            $ (j > j+1) -> {
                temp = arr[j]
                arr[j] = arr[j]
                arr[j] = temp
            }
        }
    }

    return void
}

ink factorial(num n)->num {
    $ (n == 0 or n == 1) -> { 
        return 1
    } -> {
        return n * factorial(n - 1)
    }
}

ink main()->void {
    num a, #syntax error here

    real b, c

    a = factorial(9) * 7 - 10

    b = a * 0.33

    return void
}